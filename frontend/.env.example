# Frontend Environment Example
# Copy to .env (never commit real secrets) and adjust values.

# Base URL for API calls (must include /api at end)
REACT_APP_API_URL=https://your-backend-domain.railway.app/api

# Optional: Feature flags (example placeholders)
# REACT_APP_ENABLE_EXPERIMENTAL_EXPORTS=true
# REACT_APP_SENTRY_DSN=https://public@sentry.example/1
